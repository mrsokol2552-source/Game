# Logs

Каталог для локальных логов выполнения, screenshots, traces и других диагностических артефактов.

Если артефакт нужен только на время отладки, он должен оставаться здесь, а не в `docs/` или `data/`.
